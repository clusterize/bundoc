{
  "name": "bundoc-workspace",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "bun test",
    "typecheck": "bunx tsc -p packages/bundoc --noEmit"
  },
  "overrides": {
    "@msgpack/msgpack": "3.1.2"
  }
}
